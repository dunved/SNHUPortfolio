<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#111111">

    <!-- Top bar -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="View Schedule"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:gravity="center"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:background="#1A1A1A" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Welcome + announcement box -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="#1E1E1E"
                android:padding="16dp"
                android:layout_marginBottom="12dp">

                <TextView
                    android:id="@+id/textScheduleWelcome"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Welcome! Today is loading..."
                    android:textColor="#FFFFFF"
                    android:textSize="14sp"
                    android:gravity="center" />
            </LinearLayout>

            <!-- Class list box -->
            <!-- Hint label (visible only to non-admin) -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Tap a class below to sign in."
                android:textColor="#AAAAAA"
                android:textSize="12sp"
                android:gravity="end"
                android:layout_marginBottom="4dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="#1E1E1E"
                android:padding="12dp"
                android:layout_marginBottom="16dp">

                <ListView
                    android:id="@+id/listScheduleClasses"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:divider="#2A2A2A"
                    android:dividerHeight="1dp"
                    android:nestedScrollingEnabled="false" />

                <TextView
                    android:id="@+id/textScheduleNoClasses"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="No classes scheduled."
                    android:textColor="#888888"
                    android:textSize="13sp"
                    android:visibility="gone" />
            </LinearLayout>

            <!-- Admin-only: Edit Classes button (hidden by default) -->
            <Button
                android:id="@+id/btnEditClasses"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Edit Classes (Admin)"
                android:visibility="gone"
                android:layout_marginBottom="8dp" />

            <!-- Return to Main Menu -->
            <Button
                android:id="@+id/btnReturnToMain"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Return to Main Menu"
                android:layout_marginBottom="8dp" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>